import LogoutBtn from "@/components/buttons/LogoutBtn"

const Header = () => {
  return (
    <header className="bg-gray-300">
        <div>nav</div>
        <LogoutBtn />
    </header>
  )
}

export default Header